# 🚀 คู่มือตั้งค่าแบบเร็ว (Quick Setup)

## สำหรับใช้งานบนโทรศัพท์

### ขั้นตอนที่ 1: หา IP Address

**Windows:**
```powershell
cd frontend
.\find_ip.ps1
```

**หรือใช้คำสั่ง:**
```powershell
ipconfig
```
ดูที่ **IPv4 Address** ในส่วน **WiFi** หรือ **Ethernet**

---

### ขั้นตอนที่ 2: ตั้งค่า IP Address

เปิดไฟล์: `frontend/src/config/api.js`

แก้ไขบรรทัดนี้:
```javascript
const IP_ADDRESS = '192.168.1.100'; // เปลี่ยนเป็น IP ของคุณ
```

**ตัวอย่าง:**
- ถ้า IP ของคุณคือ `192.168.1.50` → เปลี่ยนเป็น `'192.168.1.50'`
- ถ้า IP ของคุณคือ `192.168.0.100` → เปลี่ยนเป็น `'192.168.0.100'`

---

### ขั้นตอนที่ 3: ตรวจสอบ Backend

```bash
# ตรวจสอบว่า Backend รันอยู่
docker-compose ps

# ถ้ายังไม่รัน ให้รัน
docker-compose up -d
```

**ทดสอบ Backend:**
เปิดเบราว์เซอร์: `http://YOUR_IP:8000/docs`

ตัวอย่าง: `http://192.168.1.100:8000/docs`

---

### ขั้นตอนที่ 4: ตรวจสอบ Network

**ต้องแน่ใจว่า:**
- ✅ โทรศัพท์และคอมพิวเตอร์อยู่ใน **WiFi เดียวกัน**
- ✅ Backend รันอยู่ที่ port 8000
- ✅ IP address ถูกต้อง

---

### ขั้นตอนที่ 5: รันแอป

```bash
cd frontend
npm start
```

สแกน QR Code ด้วย Expo Go app

---

## ⚠️ ปัญหาที่พบบ่อย

### ❌ "ไม่สามารถเชื่อมต่อกับเซิร์ฟเวอร์ได้"

**แก้ไข:**
1. ตรวจสอบ IP address ถูกต้องหรือไม่
2. ตรวจสอบว่า Backend รันอยู่: `http://YOUR_IP:8000/docs`
3. ตรวจสอบว่าโทรศัพท์และคอมพิวเตอร์อยู่ใน WiFi เดียวกัน
4. ตรวจสอบ Firewall (Windows: เปิด port 8000)

---

## 📱 วิธีทดสอบ

1. **ทดสอบ Backend จากคอมพิวเตอร์:**
   ```
   http://YOUR_IP:8000/docs
   ```

2. **ทดสอบจากโทรศัพท์:**
   - เปิด Expo Go
   - สแกน QR Code
   - ดู Console logs

---

## 💡 Tips

- IP address อาจเปลี่ยนเมื่อ reconnect WiFi
- ถ้า IP เปลี่ยน ต้องอัปเดตใน `api.js` ใหม่
- ใช้ `find_ip.ps1` เพื่อหา IP address อัตโนมัติ
