# Postman Setup Guide - คู่มือการใช้งาน Postman

คู่มือนี้จะช่วยให้คุณทดสอบ API ด้วย Postman ได้อย่างง่ายดาย

## 📥 การ Import Collection และ Environment

### 1. Import Collection
1. เปิด Postman
2. คลิก **Import** (มุมซ้ายบน)
3. เลือกไฟล์ `Postman_Collection.json`
4. คลิก **Import**

### 2. Import Environment
1. คลิก **Environments** (มุมซ้าย)
2. คลิก **Import**
3. เลือกไฟล์ `Postman_Environment.json`
4. คลิก **Import**
5. เลือก Environment "Library System Environment" ที่มุมขวาบน

## 🔑 การตั้งค่า Environment Variables

Environment Variables จะถูกตั้งค่าอัตโนมัติเมื่อคุณ login:

- **base_url**: `http://localhost:8000` (หรือ IP address ของคุณ)
- **admin_token**: JWT token ของ Admin (ตั้งค่าอัตโนมัติเมื่อ login as admin)
- **user_token**: JWT token ของ User (ตั้งค่าอัตโนมัติเมื่อ login)
- **user_id**: ID ของผู้ใช้ (ตั้งค่าด้วยตนเอง)
- **book_id**: ID ของหนังสือ (ตั้งค่าด้วยตนเอง)
- **transaction_id**: ID ของ transaction (ตั้งค่าด้วยตนเอง)

## 🚀 วิธีใช้งาน

### ขั้นตอนที่ 1: Login เป็น Admin
1. ไปที่ **Authentication > Login as Admin**
2. คลิก **Send**
3. Token จะถูกบันทึกใน `admin_token` อัตโนมัติ

### ขั้นตอนที่ 2: สร้างหนังสือ
1. ไปที่ **Books > Create Book**
2. แก้ไข request body ตามต้องการ
3. คลิก **Send**
4. คัดลอก `id` จาก response ไปใส่ใน `book_id` variable

### ขั้นตอนที่ 3: Register และ Login เป็น User
1. ไปที่ **Authentication > Register**
2. แก้ไข username, email, password
3. คลิก **Send**
4. ไปที่ **Authentication > Login**
5. แก้ไข username และ password ให้ตรงกับที่ register
6. คลิก **Send**
7. Token จะถูกบันทึกใน `user_token` อัตโนมัติ

### ขั้นตอนที่ 4: ดูข้อมูลผู้ใช้
1. ไปที่ **Authentication > Get Current User (Me)**
2. คลิก **Send**
3. คัดลอก `id` จาก response ไปใส่ใน `user_id` variable

### ขั้นตอนที่ 5: ยืมหนังสือ
1. ไปที่ **Transactions > Borrow Book**
2. แก้ไข `user_id` และ `book_id` ใน request body
3. คลิก **Send**
4. Transaction จะถูกสร้างด้วย status = "Pending"

### ขั้นตอนที่ 6: Admin อนุมัติการยืม
1. ไปที่ **Admin > Get All Transactions**
2. คลิก **Send**
3. คัดลอก `id` ของ transaction ที่ต้องการอนุมัติ
4. ไปที่ **Admin > Approve Borrow**
5. แก้ไข `transaction_id` ใน URL
6. คลิก **Send**
7. Transaction status จะเปลี่ยนเป็น "Borrowed" และ quantity จะลดลง

### ขั้นตอนที่ 7: User คืนหนังสือ
1. ไปที่ **Transactions > Return Book**
2. แก้ไข `user_id` และ `book_id` ใน request body
3. คลิก **Send**
4. Transaction status จะเปลี่ยนเป็น "PendingReturn"

### ขั้นตอนที่ 8: Admin อนุมัติการคืน
1. ไปที่ **Admin > Approve Return**
2. แก้ไข `transaction_id` ใน URL
3. คลิก **Send**
4. Transaction status จะเปลี่ยนเป็น "Returned" และ quantity จะเพิ่มขึ้น

## 📋 Endpoints ทั้งหมด

### Authentication
- `POST /auth/register` - สมัครสมาชิก
- `POST /auth/login-json` - เข้าสู่ระบบ
- `GET /auth/me` - ดูข้อมูลผู้ใช้ปัจจุบัน

### Books
- `GET /books/` - ดึงรายการหนังสือทั้งหมด (Public)
- `GET /books/{id}` - ดึงข้อมูลหนังสือตาม ID (Public)
- `POST /books/` - สร้างหนังสือใหม่ (Admin only)
- `PUT /books/{id}` - แก้ไขข้อมูลหนังสือ (Admin only)
- `DELETE /books/{id}` - ลบหนังสือ (Admin only)

### Transactions
- `POST /transactions/borrow` - ยืมหนังสือ (สร้าง Pending)
- `POST /transactions/return` - คืนหนังสือ (เปลี่ยนเป็น PendingReturn)
- `GET /transactions/user/{user_id}` - ดูประวัติการยืม-คืน

### Admin
- `GET /admin/users` - ดึงรายการผู้ใช้ทั้งหมด
- `GET /admin/users/{user_id}` - ดึงข้อมูลผู้ใช้ตาม ID
- `PUT /admin/users/{user_id}/role` - แก้ไขบทบาทผู้ใช้
- `DELETE /admin/users/{user_id}` - ลบผู้ใช้
- `GET /admin/stats` - ดึงสถิติระบบ
- `GET /admin/transactions` - ดึงรายการ transactions ทั้งหมด
- `GET /admin/transactions/{transaction_id}` - ดึงข้อมูล transaction ตาม ID
- `POST /admin/transactions/{transaction_id}/approve-borrow` - อนุมัติการยืม
- `POST /admin/transactions/{transaction_id}/approve-return` - อนุมัติการคืน

## 🔍 การทดสอบ

### Test Scripts
Collection นี้มี Test Scripts ที่จะ:
- บันทึก token อัตโนมัติเมื่อ login สำเร็จ
- ตรวจสอบ response status code
- แสดงข้อมูลใน Console

### ตัวอย่าง Response

#### Register Response (201)
```json
{
  "id": "507f1f77bcf86cd799439011",
  "username": "testuser",
  "email": "test@example.com",
  "role": "user",
  "created_at": "2024-01-01T00:00:00"
}
```

#### Login Response (200)
```json
{
  "access_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "token_type": "bearer"
}
```

#### Create Book Response (201)
```json
{
  "id": "507f1f77bcf86cd799439012",
  "title": "หนังสือทดสอบ",
  "author": "ผู้แต่งทดสอบ",
  "isbn": "1234567890",
  "quantity": 10,
  "image_url": "http://example.com/image.jpg"
}
```

#### Borrow Response (200)
```json
{
  "id": "507f1f77bcf86cd799439013",
  "user_id": "507f1f77bcf86cd799439011",
  "book_id": "507f1f77bcf86cd799439012",
  "borrow_date": "2024-01-01T00:00:00",
  "return_date": null,
  "status": "Pending"
}
```

## ⚠️ หมายเหตุสำคัญ

1. **Token Expiration**: Token หมดอายุใน 30 นาที ต้อง login ใหม่
2. **Base URL**: ถ้าใช้ Mobile หรือเครื่องอื่น เปลี่ยน `base_url` เป็น IP address
3. **Admin Token**: ใช้สำหรับ endpoints ที่ต้องการสิทธิ์ Admin
4. **User Token**: ใช้สำหรับ endpoints ที่ต้อง login
5. **Pending Status**: การยืม-คืนต้องผ่านการอนุมัติจาก Admin ก่อน

## 🐛 Troubleshooting

### Error 401 Unauthorized
- Token หมดอายุ → Login ใหม่
- Token ไม่ถูกต้อง → ตรวจสอบว่าใช้ token ที่ถูกต้อง

### Error 403 Forbidden
- ไม่ใช่ Admin → ใช้ admin_token แทน user_token
- Token ไม่มีสิทธิ์ → ตรวจสอบ role ของผู้ใช้

### Error 404 Not Found
- ID ไม่ถูกต้อง → ตรวจสอบ ID ที่ส่งไป
- Resource ไม่มีอยู่ → สร้าง resource ก่อน

### Error 400 Bad Request
- ข้อมูลไม่ครบ → ตรวจสอบ request body
- ISBN ซ้ำ → ใช้ ISBN อื่น
- หนังสือหมด → ตรวจสอบ quantity

## 📚 เอกสารเพิ่มเติม

- API Documentation: http://localhost:8000/docs (Swagger UI)
- ReDoc: http://localhost:8000/redoc
